{
    "$schema": "http://json-schema.org/draft-07/schema#",
    "$id": "http://vte.system/pipe-envelope.schema.json",
    "title": "Pipe Envelope",
    "description": "Universal event structure for all cross-module communication in VTE.",
    "type": "object",
    "properties": {
        "workspace_id": {
            "type": "string",
            "description": "Tenant or environment identifier boundary. Hard requirement for execution."
        },
        "work_item_id": {
            "type": "string",
            "description": "Stable identifier for the end-to-end task."
        },
        "correlation_id": {
            "type": "string",
            "format": "uuid",
            "description": "Trace ID for logging and observability propagation."
        },
        "organ_source": {
            "type": "string",
            "description": "The organ issuing this event (e.g., 'brain', 'hands')."
        },
        "organ_target": {
            "type": "string",
            "description": "The destination organ."
        },
        "timestamp": {
            "type": "string",
            "format": "date-time"
        },
        "policy_version": {
            "type": "string"
        },
        "payload": {
            "type": "object",
            "description": "The organ-specific data payload that must validate against the specific pipe schema registered in the spine."
        }
    },
    "required": [
        "workspace_id",
        "work_item_id",
        "correlation_id",
        "organ_source",
        "timestamp",
        "payload"
    ],
    "additionalProperties": false
}