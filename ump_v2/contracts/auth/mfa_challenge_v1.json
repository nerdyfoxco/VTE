{
  "description": "Contract generated by the server when a SignIn requires TOTP verification.",
  "properties": {
    "mfa_token_id": {
      "description": "Temporary token claiming the pre-auth session.",
      "title": "Mfa Token Id",
      "type": "string"
    },
    "challenge_type": {
      "default": "TOTP",
      "title": "Challenge Type",
      "type": "string"
    }
  },
  "required": [
    "mfa_token_id"
  ],
  "title": "MFAChallenge",
  "type": "object"
}